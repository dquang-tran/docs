For [Custom Use Case](/custom-use-cases), FleetWorks sends to your service `tool_calls` events during the call and `call_ended` event after the outbound call finished.

## Call Ended Event Payload

<ResponseField name="event" type="string" required>
  Event type: `call_ended`
</ResponseField>

<ResponseField name="callId" type="number" required>
  Unique call identifier
</ResponseField>

<ResponseField name="createdAt" type="string" required>
  Call created time
</ResponseField>

<ResponseField name="recordingUrl" type="string">
  URL to download call recording
</ResponseField>

<ResponseField name="formattedTranscript" type="string">
  Text transcription of the call
</ResponseField>

<ResponseField name="transcript" type="TranscriptMessage[]">
  <Expandable title="properties">
    <ResponseField name="role" type="string">
      The role of the message in the conversation: `user`, `bot`
    </ResponseField>
    
    <ResponseField name="message" type="string">
      The message content
    </ResponseField>

    <ResponseField name="time" type="number">
      The timestamp when the message was sent
    </ResponseField>

    <ResponseField name="endTime" type="number">
      The timestamp when the message ended
    </ResponseField>

    <ResponseField name="secondsFromStart" type="number">
      The number of seconds from the start of the conversation
    </ResponseField>

    <ResponseField name="duration" type="number">
      The duration of the message in seconds
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="extractedData" type="object">
  Extraction data from call's transcript
</ResponseField>

## Tool Calls Event Payload

<ResponseField name="event" type="string" required>
  Event type: `tool_calls`
</ResponseField>

<ResponseField name="callId" type="number" required>
  Unique call identifier
</ResponseField>

<ResponseField name="createdAt" type="string" required>
  Call created time
</ResponseField>

<ResponseField name="function" type="ToolCallFunction">
  <Expandable title="properties">
    <ResponseField name="name" type="string">
      Tool call function name
    </ResponseField>

    <ResponseField name="arguments" type="object">
      Tool call function arguments
    </ResponseField>
  </Expandable>
</ResponseField>
